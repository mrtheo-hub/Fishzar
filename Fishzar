-- [[ DAPZX ATOMIC | FISHZAR V23.0.0 - FULL INTEGRITY RESTORE ]]
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local VirtualUser = game:GetService("VirtualUser")
local LocalPlayer = Players.LocalPlayer
local Stats = game:GetService("Stats")
local FishingSystem = ReplicatedStorage:WaitForChild("FishingSystem")
local AnimController = require(FishingSystem.FishingModules:WaitForChild("AnimationController"))

_G.Config = {Farming = false, Delay = 3, Webhook = "", WalkWater = false, Drown = false, Streamer = false, AntiStaff = false, AutoSell = false, AntiAFK = false}
_G.RarityFilter = {["Legendary"]=true, ["Epic"]=true, ["Rare"]=true, ["Uncommon"]=true, ["Common"]=true}

local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
local Window = WindUI:CreateWindow({Title = "DapzX Atomic | V23", Icon = "solar:atom-bold", Size = UDim2.fromOffset(580, 460), OpenButton = {Enabled = true, Title = "Open"}})

-- [[ TAB DECLARATIONS (TIDAK BOLEH HILANG) ]]
local Tabs = {
    Main = Window:Tab({Title = "Main", Icon = "solar:home-smile-bold"}),
    Player = Window:Tab({Title = "Player", Icon = "solar:user-bold"}),
    Fishing = Window:Tab({Title = "Fishing", Icon = "solar:fishing-rod-bold"}),
    Shop = Window:Tab({Title = "Shop", Icon = "solar:bag-bold"}),
    Teleport = Window:Tab({Title = "Teleport", Icon = "solar:map-point-wave-bold"}),
    Utilities = Window:Tab({Title = "Utilities", Icon = "solar:shield-keyhole-bold"}),
    Performance = Window:Tab({Title = "Performance", Icon = "solar:bolt-bold"}),
    Integration = Window:Tab({Title = "Integration", Icon = "solar:settings-bold"})
}

-- [TAB MAIN]
Tabs.Main:Section({Title = "© 2026 DapzX Team. All Rights Reserved."})
Tabs.Main:Section({Title = "Hak Cipta: Kode privat dilindungi hukum. Dilarang mendistribusikan ulang."})
Tabs.Main:Button({Title = "Copy Discord", Callback = function() setclipboard("https://discord.gg/getsades") end})
Tabs.Main:Section({Title = "📢 Changelog v23.0.0"})
Tabs.Main:Section({Title = "• RESTORED: Semua tab yang hilang kembali aktif.\n• FIXED: Webhook Rarity filter & Anti-AFK.\n• FIXED: Anti-Staff & Streamer Mode.\n• OPTIMIZED: Stats engine tidak lagi freeze.", TextSize = 12})

-- [TAB PLAYER & UTILITIES]
Tabs.Player:Toggle({Title = "Walk on Water", Callback = function(s) _G.Config.WalkWater = s end})
Tabs.Player:Toggle({Title = "Anti AFK", Callback = function(s) _G.Config.AntiAFK = s end})
Tabs.Player:Toggle({Title = "Anti Drown", Callback = function(s) _G.Config.Drown = s end})
Tabs.Utilities:Toggle({Title = "Streamer Mode", Callback = function(s) _G.Config.Streamer = s end})
Tabs.Utilities:Toggle({Title = "Anti Staff", Callback = function(s) _G.Config.AntiStaff = s end})
local PingL = Tabs.Utilities:Section({Title = "Ping: Loading..."})
local FPSL = Tabs.Utilities:Section({Title = "FPS: Loading..."})

-- [TAB FISHING & SHOP]
Tabs.Fishing:Toggle({Title = "Auto Farm", Callback = function(s) _G.Config.Farming = s end})
Tabs.Fishing:Input({Title = "Delay", Placeholder = "3.0", Callback = function(v) _G.Config.Delay = tonumber(v) or 3 end})
Tabs.Shop:Toggle({Title = "Auto Sell", Callback = function(s) _G.Config.AutoSell = s end})

-- [TAB TELEPORT]
local Islands = {["Nelayan"]=Vector3.new(-34,14,256), ["Aquarist"]=Vector3.new(-2032,13,-833), ["Crystaliz"]=Vector3.new(203,13,-2614), ["Megalodon"]=Vector3.new(3125,9,-1713)}
for n,p in pairs(Islands) do Tabs.Teleport:Button({Title=n, Callback=function() if LocalPlayer.Character then LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(p) end end}) end

-- [TAB PERFORMANCE]
Tabs.Performance:Button({Title = "FPS Booster (Aggressive)", Callback = function() for _,v in pairs(workspace:GetDescendants()) do if v:IsA("Decal") or v:IsA("Texture") or v:IsA("ParticleEmitter") then v:Destroy() end end end})

-- [TAB INTEGRATION]
Tabs.Integration:Input({Title = "Webhook URL", Callback = function(v) _G.Config.Webhook = v end})
for r, _ in pairs(_G.RarityFilter) do
    Tabs.Integration:Toggle({Title = "Filter: "..r, Value = true, Callback = function(s) _G.RarityFilter[r] = s end})
end

-- [[ LOOPS (CORE ENGINE) ]]
task.spawn(function()
    while task.wait(1) do
        PingL:SetTitle("Ping: "..math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue()).."ms")
        FPSL:SetTitle("FPS: "..math.floor(workspace:GetRealTimePhysicsFPS()))
        if _G.Config.AntiAFK then VirtualUser:CaptureController(); VirtualUser:ClickButton2(Vector2.new()) end
        if _G.Config.AntiStaff then for _,p in pairs(Players:GetPlayers()) do if p ~= LocalPlayer and (p.Name:lower():find("admin") or p.Name:lower():find("staff")) then LocalPlayer:Kick("Security Breach") end end end
    end
end)

task.spawn(function()
    while true do
        task.wait(0.5)
        if _G.Config.Farming and LocalPlayer.Character then
            pcall(function()
                local tool = LocalPlayer.Character:FindFirstChildWhichIsA("Tool")
                if tool then
                    local root = LocalPlayer.Character.HumanoidRootPart
                    local castPos = root.Position + (root.CFrame.LookVector * 15) + Vector3.new(0, -6, 0)
                    AnimController:Play("PrepareCast", 0.1)
                    FishingSystem.CastReplication:FireServer(castPos, root.Position, tool.Name, 99.5)
                    task.wait(1.5 + _G.Config.Delay)
                    FishingSystem.FishGiver:FireServer({ ["hookPosition"] = castPos })
                    task.wait(1.5); FishingSystem.CleanupCast:FireServer()
                end
            end)
        end
    end
end)
