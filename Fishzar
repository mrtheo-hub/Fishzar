local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local Executor = (identifyexecutor and identifyexecutor()) or "Unknown"
local ScriptVersion = "v14.0.9 | STABLE-FIX"

-- [ CLEANUP ]
for _, v in pairs(game:GetService("CoreGui"):GetChildren()) do
    if v.Name == "WindUI" or v.Name == "DapzXStats" then v:Destroy() end
end

local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

-- [ GLOBAL CONFIG ]
_G.FarmingActive = false
_G.AutoSellActive = false
_G.WalkOnWater = false
_G.AntiDrown = false
_G.AntiAFK = false
_G.StreamerMode = false
_G.MinigameDelay = 3.0
_G.WebhookURL = ""
_G.Filters = {Common = true, Uncommon = true, Rare = true, Epic = true, Legendary = true}

local Window = WindUI:CreateWindow({
    Title = "DapzX Atomic | Fishzar",
    Author = "by Alfer1427",
    Icon = "solar:atom-bold",
    Size = UDim2.fromOffset(580, 460)
})

Window:Tag({ Title = Executor .. " | " .. ScriptVersion, Color = Color3.fromHex("#00B4D8") })

-- 1. MAIN
local MainTab = Window:Tab({ Title = "Main", Icon = "solar:home-bold" })
MainTab:Button({ Title = "Copy Discord (getsades)", Callback = function() setclipboard("https://discord.gg/getsades") end })
MainTab:Button({ Title = "Check Executor", Callback = function() WindUI:Notify({Title = "Executor", Content = "Running on: "..Executor}) end })

-- 2. INFO (Pindah ke bawah Main)
local InfoTab = Window:Tab({ Title = "Info", Icon = "solar:info-square-bold" })
InfoTab:Section({ Title = "Script Information" })
InfoTab:Section({ Title = "Version: " .. ScriptVersion })
InfoTab:Section({ Title = "Developer: Alfer1427" })
InfoTab:Section({ Title = "Team: DapzX Team & ATMC" })

-- 3. PLAYER
local PlayerTab = Window:Tab({ Title = "Player", Icon = "solar:user-rounded-bold" })
PlayerTab:Toggle({ Title = "Walk on Water", Callback = function(v) _G.WalkOnWater = v end })
PlayerTab:Toggle({ Title = "Anti Drown", Callback = function(v) _G.AntiDrown = v end })
PlayerTab:Toggle({ Title = "Anti AFK", Callback = function(v) _G.AntiAFK = v end })

-- 4. FISHING
local FishingTab = Window:Tab({ Title = "Fishing", Icon = "solar:cursor-square-bold" })
FishingTab:Toggle({ Title = "Auto Farm (Classic)", Callback = function(v) _G.FarmingActive = v end })
FishingTab:Toggle({ Title = "Auto Sell", Callback = function(v) _G.AutoSellActive = v end })
FishingTab:Input({ Title = "Fishing Delay", Placeholder = "3.0", Callback = function(v) _G.MinigameDelay = tonumber(v) or 3.0 end })

-- 5. TELEPORT
local TeleportTab = Window:Tab({ Title = "Teleport", Icon = "solar:map-point-wave-bold" })
TeleportTab:Dropdown({ 
    Title = "Select Island", 
    Values = {"Pulau Nelayan", "Pulau Aquarist", "Pulau Criystializ"}, 
    Callback = function(v)
        local locs = {["Pulau Nelayan"] = Vector3.new(-34, 14, 256), ["Pulau Aquarist"] = Vector3.new(-2032, 13, -833), ["Pulau Criystializ"] = Vector3.new(203, 13, -2614)}
        if locs[v] then LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(locs[v] + Vector3.new(0,5,0)) end
    end 
})
TeleportTab:Dropdown({ 
    Title = "Teleport to Player", 
    Values = (function() local t = {} for _,p in pairs(Players:GetPlayers()) do if p ~= LocalPlayer then table.insert(t, p.Name) end end return t end)(), 
    Callback = function(v)
        local target = Players:FindFirstChild(v)
        if target and target.Character then LocalPlayer.Character.HumanoidRootPart.CFrame = target.Character.HumanoidRootPart.CFrame end
    end 
})

-- 6. WEBHOOK
local WebhookTab = Window:Tab({ Title = "Webhook", Icon = "solar:bell-bing-bold" })
WebhookTab:Input({ Title = "Webhook URL", Callback = function(v) _G.WebhookURL = v end })
WebhookTab:Section({ Title = "Rarity Filter" })
for i, _ in pairs(_G.Filters) do
    WebhookTab:Toggle({ Title = "Filter: "..i, Value = true, Callback = function(v) _G.Filters[i] = v end })
end

-- 7. SHOP
local ShopTab = Window:Tab({ Title = "Shop", Icon = "solar:cart-bold" })
ShopTab:Input({ Title = "Buy Item Name", Callback = function(v) end })
ShopTab:Button({ Title = "Buy Best Fishing Rod", Callback = function() end })

-- 8. UTILITIES
local UtilTab = Window:Tab({ Title = "Utilities", Icon = "solar:shield-check-bold" })
UtilTab:Toggle({ Title = "Streamer Mode", Callback = function(v) _G.StreamerMode = v end })
UtilTab:Button({ Title = "Re-join Server", Callback = function() game:GetService("TeleportService"):Teleport(game.PlaceId, LocalPlayer) end })

-- 9. PERFORMANCE
local PerfTab = Window:Tab({ Title = "Performance", Icon = "solar:monitor-camera-bold" })
PerfTab:Button({ Title = "FPS Boost (Aggressive)", Callback = function() 
    for _,v in pairs(game:GetDescendants()) do 
        if v:IsA("BasePart") then v.Material = Enum.Material.SmoothPlastic v.CastShadow = false
        elseif v:IsA("Decal") or v:IsA("Texture") then v:Destroy() end
    end 
end })

-- [ CORE BACKGROUND LOOP ]
task.spawn(function()
    local WoW = Instance.new("Part", workspace)
    WoW.Size = Vector3.new(30, 1, 30)
    WoW.Anchored, WoW.Transparency, WoW.CanCollide = true, 1, false
    
    local VU = game:GetService("VirtualUser")
    
    while task.wait(0.5) do
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            -- Streamer Fix
            if _G.StreamerMode then
                char.Humanoid.DisplayName = "Streamer"
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= LocalPlayer and p.Character then p.Character.Humanoid.DisplayName = "Player" end
                end
            end
            -- Anti Drown Fix
            if _G.AntiDrown then
                local ox = char:FindFirstChild("Oxygen") or char:FindFirstChild("Breath")
                if ox then ox.Value = 100 end
            end
            -- WoW Fix
            if _G.WalkOnWater then
                WoW.CanCollide = true
                WoW.CFrame = CFrame.new(char.HumanoidRootPart.Position.X, 13.8, char.HumanoidRootPart.Position.Z)
            else
                WoW.CanCollide = false
            end
            -- Anti AFK Fix
            if _G.AntiAFK then
                VU:CaptureController()
                VU:ClickButton2(Vector2.new())
            end
        end
    end
end)

-- [ FISHING & AUTO SELL ]
task.spawn(function()
    local FS = ReplicatedStorage:WaitForChild("FishingSystem", 10)
    while task.wait(0.5) do
        if _G.FarmingActive and FS then
            pcall(function()
                local char = LocalPlayer.Character
                local rod = char:FindFirstChildWhichIsA("Tool")
                if rod then
                    local root = char.HumanoidRootPart
                    local castPos = root.Position + (root.CFrame.LookVector * 15)
                    FS.CastReplication:FireServer(castPos, root.Position, rod.Name, 100)
                    task.wait(2)
                    FS.PrecalcFish:InvokeServer()
                    task.wait(_G.MinigameDelay)
                    FS.FishGiver:FireServer({["hookPosition"] = castPos})
                    FS.CleanupCast:FireServer()
                end
            end)
        end
        if _G.AutoSellActive and FS then
            local sell = FS:FindFirstChild("SellFish")
            if sell then sell:FireServer("SellAllBatch", LocalPlayer.Backpack:GetChildren()) end
        end
    end
end)

WindUI:Notify({Title = "DapzX Atomic", Content = "v14.0.9 Loaded!", Duration = 3})
