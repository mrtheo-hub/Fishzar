-- [[ FLOW HUB | FISHING SYSTEM V2.0.0 REBORN ]]
-- Owner: Dapz | Developer: Theo & Mon
-- TIDAK ADA SANGKUT PAUT DENGAN ATOMIC, FULLY MILIK FLOW HUB.

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local Lighting = game:GetService("Lighting")

local LocalPlayer = Players.LocalPlayer
local FishingSystem = ReplicatedStorage:WaitForChild("FishingSystem", 10)
local AnimController = FishingSystem and require(FishingSystem.FishingModules:WaitForChild("AnimationController"))
local request = (syn and syn.request) or (http and http.request) or http_request or request or HttpService.RequestAsync

-- [ CONFIGURATION ]
getgenv().FlowConfig = {
    Farming = false, AutoSell = false, WalkWater = false, AntiDrown = false,
    AutoEquip = false, WebhookURL = "", WebhookDisconnect = false,
    WebhookFilters = {Common=false, Uncommon=false, Rare=false, Epic=true, Legendary=true, Secret=true}
}

local Islands = {
    ["Pulau Nelayan"] = Vector3.new(-34, 14, 256), ["Pulau Aquarist"] = Vector3.new(-2032, 13, -833),
    ["Pulau Crystaliz"] = Vector3.new(214, 14, -2706), ["Gua Crystaliz"] = Vector3.new(171, -142, -4938),
    ["Pulau Pedalaman Posei"] = Vector3.new(-861, -367, -1283), ["Megalodon Zone"] = Vector3.new(3125, 9, -1713)
}

-- [[ ABSOLUTE ANTI-AFK ]]
local function EnableAntiAFK()
    local getConnections = getconnections or get_signal_cons
    if getConnections then
        pcall(function()
            for _, connection in pairs(getConnections(LocalPlayer.Idled)) do
                if connection["Disable"] then connection:Disable()
                elseif connection["Disconnect"] then connection:Disconnect() end
            end
        end)
    end
end
EnableAntiAFK()

-- [[ STARLIGHT UI SETUP ]]
-- Menggunakan Starlight Library untuk tampilan premium & ringan di HP
local Starlight = loadstring(game:HttpGet("https://raw.githubusercontent.com/1201for/V.G-Hub/main/Starlight"))()

local Window = Starlight:MakeWindow({
    Name = "Flow Hub | v2.0.0 Reborn",
    HidePremium = false,
    SaveConfig = true,
    ConfigFolder = "FlowHubConfig"
})

-- [ TABS ]
local TabMain = Window:MakeTab({Name = "Main Farm", Icon = "rbxassetid://4483345998", PremiumOnly = false})
local TabPlayer = Window:MakeTab({Name = "Player", Icon = "rbxassetid://4483367336", PremiumOnly = false})
local TabTeleport = Window:MakeTab({Name = "Teleports", Icon = "rbxassetid://4483345998", PremiumOnly = false})
local TabMisc = Window:MakeTab({Name = "Misc & FPS", Icon = "rbxassetid://4483345998", PremiumOnly = false})

-- [[ TAB: MAIN FARM ]]
local SectionFarm = TabMain:AddSection({Name = "Fishing Automation"})

TabMain:AddToggle({
    Name = "Auto Farm (Fast Mode)",
    Default = false,
    Callback = function(Value)
        FlowConfig.Farming = Value
    end    
})

TabMain:AddToggle({
    Name = "Auto Sell All Fishes",
    Default = false,
    Callback = function(Value)
        FlowConfig.AutoSell = Value
    end    
})

local SectionInfo = TabMain:AddSection({Name = "Project Info"})
TabMain:AddParagraph("Credits", "Owner: Dapz\nDeveloper: Theo & Mon")
TabMain:AddParagraph("Status", "Fully Optimized for Mobile Execution")

-- [[ TAB: PLAYER ]]
local SectionPlayer = TabPlayer:AddSection({Name = "Character Modifications"})
local lockedY = nil
local waterPart = Instance.new("Part", workspace)
waterPart.Name = "FlowWaterPlatform"; waterPart.Size = Vector3.new(20, 1, 20)
waterPart.Transparency = 1; waterPart.Anchored = true; waterPart.CanCollide = true

TabPlayer:AddToggle({
    Name = "Walk on Water",
    Default = false,
    Callback = function(Value)
        FlowConfig.WalkWater = Value
        if Value and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            lockedY = LocalPlayer.Character.HumanoidRootPart.Position.Y - 3.2
        else 
            lockedY = nil 
            waterPart.CFrame = CFrame.new(0, -9999, 0)
        end
    end    
})

TabPlayer:AddToggle({ Name = "Anti Drown", Default = false, Callback = function(Value) FlowConfig.AntiDrown = Value end })
TabPlayer:AddToggle({ Name = "Auto Equip Rod", Default = false, Callback = function(Value) FlowConfig.AutoEquip = Value end })

-- [[ TAB: TELEPORT ]]
local SectionIslands = TabTeleport:AddSection({Name = "Island Navigation"})
local islandNames = {} for name, _ in pairs(Islands) do table.insert(islandNames, name) end

TabTeleport:AddDropdown({
    Name = "Select Island",
    Default = "",
    Options = islandNames,
    Callback = function(Value)
        if Islands[Value] and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then 
            LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(Islands[Value] + Vector3.new(0, 5, 0)) 
        end
    end    
})

-- [[ TAB: MISC & FPS ]]
local SectionFPS = TabMisc:AddSection({Name = "Performance Booster"})

TabMisc:AddButton({
    Name = "Super FPS Boost (Potato PC/HP)",
    Callback = function()
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 9e9
        settings().Rendering.QualityLevel = 1
        for _,v in pairs(workspace:GetDescendants()) do
            if v:IsA("BasePart") then v.Material = Enum.Material.SmoothPlastic; v.CastShadow = false
            elseif v:IsA("Texture") or v:IsA("Decal") or v:IsA("ParticleEmitter") then v:Destroy() end
        end
    end    
})

TabMisc:AddButton({
    Name = "Rejoin Server",
    Callback = function()
        if #Players:GetPlayers() <= 1 then TeleportService:Teleport(game.PlaceId, LocalPlayer) else TeleportService:TeleportToPlaceInstance(game.PlaceId, game.JobId, LocalPlayer) end
    end
})

-- [[ CORE LOGIC LOOPS (OPTIMIZED) ]]

-- Loop 1: Movement & Water Walk (High Frequency - Heartbeat)
getgenv().FlowConnection = RunService.Heartbeat:Connect(function()
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        if FlowConfig.WalkWater and lockedY then
            waterPart.CFrame = CFrame.new(char.HumanoidRootPart.Position.X, lockedY, char.HumanoidRootPart.Position.Z)
        end
    end
end)

-- Loop 2: Utilities & Auto Sell (Low Frequency - Hemat CPU)
task.spawn(function()
    while task.wait(1.5) do
        local char = LocalPlayer.Character
        if not char then continue end
        local hum = char:FindFirstChild("Humanoid")
        
        -- Auto Sell Safe Routine
        if FlowConfig.AutoSell then
            local sellRemote = ReplicatedStorage:FindFirstChild("SellFish", true) or ReplicatedStorage:FindFirstChild("Sell", true)
            if sellRemote then
                for _, item in pairs(LocalPlayer.Backpack:GetChildren()) do
                    if item:IsA("Tool") and not item.Name:lower():find("rod") then
                        pcall(function()
                            if sellRemote:IsA("RemoteFunction") then sellRemote:InvokeServer()
                            elseif sellRemote:IsA("RemoteEvent") then sellRemote:FireServer(item.Name) end
                        end)
                        task.wait(0.05) -- Micro-delay to prevent limits
                    end
                end
            end
        end

        -- Anti Drown & Auto Equip
        if hum then
            if FlowConfig.AntiDrown then
                hum:SetStateEnabled(Enum.HumanoidStateType.Swimming, false)
                for _, v in pairs(char:GetDescendants()) do
                    if (v.Name:lower() == "oxygen" or v.Name:lower() == "o2") and v:IsA("NumberValue") then v.Value = 100 end
                end
            else
                hum:SetStateEnabled(Enum.HumanoidStateType.Swimming, true)
            end

            if FlowConfig.AutoEquip and not char:FindFirstChildWhichIsA("Tool") then
                for _, item in pairs(LocalPlayer.Backpack:GetChildren()) do
                    if item:IsA("Tool") and (item.Name:lower():find("rod") or item.Name:lower():find("kail")) then
                        hum:EquipTool(item); break
                    end
                end
            end
        end
    end
end)

-- Loop 3: Fast Fishing Engine (Medium Frequency)
task.spawn(function()
    while task.wait(0.2) do
        if FlowConfig.Farming and LocalPlayer.Character and FishingSystem then
            pcall(function()
                local tool = LocalPlayer.Character:FindFirstChildWhichIsA("Tool")
                if tool and (tool.Name:lower():find("rod") or tool.Name:lower():find("kail")) then
                    local root = LocalPlayer.Character.HumanoidRootPart
                    local castPos = root.Position + (root.CFrame.LookVector * 15)
                    
                    if AnimController then AnimController:Play("PrepareCast", 0.1) end
                    FishingSystem.CastReplication:FireServer(castPos, root.Position, tool.Name, 99.5)
                    
                    task.wait(math.random(5, 9) / 10) -- Randomize 0.5s - 0.9s
                    FishingSystem.PrecalcFish:InvokeServer()
                    
                    FishingSystem.ReplicatePullAlert:FireServer("rbxassetid://76503247176490")
                    
                    task.wait(math.random(8, 12) / 10) -- Randomize 0.8s - 1.2s
                    FishingSystem.FishGiver:FireServer({["hookPosition"]=castPos})
                    
                    task.wait(0.3)
                    FishingSystem.CleanupCast:FireServer()
                end
            end)
        end
    end
end)

Starlight:Init()
