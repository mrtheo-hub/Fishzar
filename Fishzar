-- DAPZX ATOMIC | FINAL SURGICAL PATCH
local RunService = game:GetService("RunService")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")

local Executor = (identifyexecutor and identifyexecutor()) or "Unknown"
local ScriptVersion = "v14.0.7 | STABLE"

-- [UI INITIALIZATION - DIBIARKAN TETAP]
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "DapzX Atomic | Fishzar",
    Author = "by Alfer1427",
    Icon = "solar:atom-bold",
    Size = UDim2.fromOffset(580, 460)
})

-- [TAG EXECUTOR - DIBIARKAN TETAP]
Window:Tag({ Title = Executor .. " | " .. ScriptVersion, Color = Color3.fromHex("#00B4D8") })

-- [TABS - DIBIARKAN TETAP]
local MainTab = Window:Tab({ Title = "Main", Icon = "solar:home-bold" })
local PlayerTab = Window:Tab({ Title = "Player", Icon = "solar:user-rounded-bold" })
local FishingTab = Window:Tab({ Title = "Fishing", Icon = "solar:cursor-square-bold" })
local TeleportTab = Window:Tab({ Title = "Teleport", Icon = "solar:map-point-wave-bold" })
local WebhookTab = Window:Tab({ Title = "Webhook", Icon = "solar:bell-bing-bold" })
local ShopTab = Window:Tab({ Title = "Shop", Icon = "solar:cart-bold" })
local UtilTab = Window:Tab({ Title = "Utilities", Icon = "solar:shield-check-bold" })
local PerfTab = Window:Tab({ Title = "Performance", Icon = "solar:monitor-camera-bold" })

-- [LOGIC A, B, C - INTEGRASI SURGICAL]
_G.StreamerMode = false
_G.AntiDrown = false
_G.WebhookURL = ""
_G.FarmingActive = false
_G.MinigameDelay = 3.0

-- FIX B: Webhook Function
local function SendWebhook(msg)
    if _G.WebhookURL == "" then return end
    local Proxy = _G.WebhookURL:gsub("discord.com", "webhook.lewisakura.moe")
    pcall(function()
        request({
            Url = Proxy,
            Method = "POST",
            Headers = {["Content-Type"] = "application/json"},
            Body = HttpService:JSONEncode({["content"] = msg})
        })
    end)
end

-- [MAIN BACKGROUND LOOP - INTEGRASI FIX]
task.spawn(function()
    while true do
        task.wait(0.5)
        
        -- FIX A: Streamer Mode
        if _G.StreamerMode and LocalPlayer.Character then
            LocalPlayer.Character.Humanoid.DisplayName = "Streamer"
            for _, p in pairs(Players:GetPlayers()) do
                if p ~= LocalPlayer and p.Character and p.Character:FindFirstChild("Head") then
                    p.Character.Humanoid.DisplayName = "Player"
                    for _, v in pairs(p.Character.Head:GetChildren()) do
                        if v:IsA("BillboardGui") then v.Enabled = false end
                    end
                end
            end
        end

        -- FIX C: Anti Drown
        if _G.AntiDrown and LocalPlayer.Character then
            local ox = LocalPlayer.Character:FindFirstChild("Oxygen") or LocalPlayer.Character:FindFirstChild("Breath")
            if ox then ox.Value = 100 end
            for _, v in pairs(LocalPlayer.Character:GetDescendants()) do
                if v:IsA("LocalScript") and v.Name:lower():find("drown") then v.Disabled = true end
            end
        end
    end
end)

-- [ORIGINAL FISHING METHOD - DIBIARKAN TETAP]
task.spawn(function()
    local FS = ReplicatedStorage:WaitForChild("FishingSystem", 10)
    while true do
        task.wait(0.5)
        if _G.FarmingActive and FS then
            pcall(function()
                local char = LocalPlayer.Character
                local rod = char:FindFirstChildWhichIsA("Tool")
                if rod then
                    local root = char.HumanoidRootPart
                    local castPos = root.Position + (root.CFrame.LookVector * 15)
                    FS.CastReplication:FireServer(castPos, root.Position, rod.Name, 100)
                    task.wait(2)
                    FS.PrecalcFish:InvokeServer()
                    task.wait(_G.MinigameDelay)
                    FS.FishGiver:FireServer({["hookPosition"] = castPos})
                    FS.CleanupCast:FireServer()
                end
            end)
        end
    end
end)

-- [UI SETTINGS - DIBIARKAN TETAP]
PlayerTab:Toggle({ Title = "Walk on Water", Callback = function(v) _G.WalkOnWater = v end })
PlayerTab:Toggle({ Title = "Anti Drown", Callback = function(v) _G.AntiDrown = v end })
FishingTab:Toggle({ Title = "Auto Farm (Classic)", Callback = function(v) _G.FarmingActive = v end })
FishingTab:Input({ Title = "Delay", Placeholder = "3.0", Callback = function(v) _G.MinigameDelay = tonumber(v) or 3.0 end })
WebhookTab:Input({ Title = "Webhook URL", Callback = function(v) _G.WebhookURL = v end })
UtilTab:Toggle({ Title = "Streamer Mode", Callback = function(v) _G.StreamerMode = v end })

print("✅ Script v14.0.7 Loaded (Surgical Fix Applied)")
