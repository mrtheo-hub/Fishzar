-- [[ FLOW HUB | FISHING SYSTEM V1.2.2 (OPTIMIZED) ]]
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local HttpService = game:GetService("HttpService")
local Stats = game:GetService("Stats")
local VirtualUser = game:GetService("VirtualUser")

-- [ CLEANUP PADA SAAT RE-EXECUTE ]
if game.CoreGui:FindFirstChild("FlowStatsV12") then game.CoreGui.FlowStatsV12:Destroy() end
if workspace:FindFirstChild("FlowWaterPlatform") then workspace.FlowWaterPlatform:Destroy() end
if getgenv().FlowConnection then getgenv().FlowConnection:Disconnect() end

local FishingSystem = ReplicatedStorage:WaitForChild("FishingSystem", 10)
local AnimController = FishingSystem and require(FishingSystem.FishingModules:WaitForChild("AnimationController"))
local request = (syn and syn.request) or (http and http.request) or http_request or request or HttpService.RequestAsync

-- [[ CONFIGURATION ]]
getgenv().FlowConfig = {
    Farming = false, Delay = 3.0, AutoSell = false, WalkWater = false, AntiDrown = false,
    AutoEquip = false, Streamer = false, AntiStaff = false, WebhookURL = "",
    WebhookFilters = {Common=false, Uncommon=false, Rare=false, Epic=true, Legendary=true, Secret=true, Mythic=true}
}

-- [[ UI STATS LOGIC ]]
local StatsGui = Instance.new("ScreenGui", game.CoreGui)
StatsGui.Name = "FlowStatsV12"
StatsGui.Enabled = false

local StatsFrame = Instance.new("Frame", StatsGui)
StatsFrame.Size = UDim2.new(0, 220, 0, 45)
StatsFrame.Position = UDim2.new(0.5, -110, 0, 15)
StatsFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
StatsFrame.Active = true
StatsFrame.Draggable = true -- Bawaan executor UI drag
Instance.new("UICorner", StatsFrame).CornerRadius = UDim.new(0, 10)
Instance.new("UIStroke", StatsFrame).Color = Color3.fromRGB(0, 180, 216)

local PingL = Instance.new("TextLabel", StatsFrame)
PingL.Size = UDim2.new(0.5, 0, 1, 0)
PingL.Position = UDim2.new(0, 12, 0, 0)
PingL.BackgroundTransparency = 1
PingL.TextColor3 = Color3.new(1,1,1)
PingL.Font = Enum.Font.GothamBold
PingL.TextSize = 14
PingL.TextXAlignment = Enum.TextXAlignment.Left

local FPSL = Instance.new("TextLabel", StatsFrame)
FPSL.Size = UDim2.new(0.5, -12, 1, 0)
FPSL.Position = UDim2.new(0.5, 0, 0, 0)
FPSL.BackgroundTransparency = 1
FPSL.TextColor3 = Color3.new(1,1,1)
FPSL.Font = Enum.Font.GothamBold
FPSL.TextSize = 14
FPSL.TextXAlignment = Enum.TextXAlignment.Right

task.spawn(function()
    local lastTick, frames = tick(), 0
    while task.wait() do
        frames = frames + 1
        if tick() - lastTick >= 0.5 then
            FPSL.Text = "FPS: " .. math.floor(frames / 0.5)
            PingL.Text = "Ping: " .. math.floor(Stats.Network.ServerStatsItem["Data Ping"]:GetValue())
            frames = 0
            lastTick = tick()
        end
    end
end)

-- [[ WINDUI LOAD ]]
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()
local execName = identifyexecutor and identifyexecutor() or "Unknown Executor"

local Window = WindUI:CreateWindow({
    Title = "Flow Hub | Fishing (" .. execName .. ")",
    Folder = "FlowHubV12",
    Icon = "solar:waterdrop-bold",
    Size = UDim2.fromOffset(600, 480),
    OpenButton = { Enabled = true, Title = "Open Flow", Color = ColorSequence.new(Color3.fromHex("#00B4D8"), Color3.fromHex("#03045E")) }
})

Window.MinimizeKey = Enum.KeyCode.RightControl

local Tabs = {
    Main = Window:Tab({ Title = "Main", Icon = "solar:home-bold" }),
    Player = Window:Tab({ Title = "Player", Icon = "solar:user-rounded-bold" }),
    Fishing = Window:Tab({ Title = "Fishing", Icon = "solar:fishing-rod-bold" }),
    Teleport = Window:Tab({ Title = "Teleport", Icon = "solar:map-point-wave-bold" }),
    Webhook = Window:Tab({ Title = "Webhook", Icon = "solar:bell-bing-bold" }),
    Performance = Window:Tab({ Title = "Performance", Icon = "solar:bolt-bold" })
}

-- [MAIN]
Tabs.Main:Section({ Title = "System Info" })
Tabs.Main:Section({ Title = "• Author: Alfer1427\n• Version: 1.2.2 Stable\n• Executor: " .. execName, TextSize = 13 })

Tabs.Main:Section({ Title = "📜 Changelog v1.2.2" })
Tabs.Main:Paragraph({
    Title = "Engine Rewritten:",
    Content = "- [FIX] Walk on Water anti-bouncing & cave support\n- [FIX] Memory leaks in Auto Sell removed\n- [FIX] Webhook requests logic optimized\n- [NEW] Performance loop separated from Physics"
})

Tabs.Main:Button({
    Title = "Discord Server",
    Icon = "solar:link-bold",
    Callback = function()
        setclipboard("https://discord.gg/wVr9r3tCmT")
        WindUI:Notify({Title="Discord", Content="Link copied to clipboard!", Duration=3})
    end
})

-- [PLAYER]
local lockedY = nil
Tabs.Player:Toggle({ 
    Title = "Walk on Water", 
    Callback = function(v) 
        FlowConfig.WalkWater = v 
        if v and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            lockedY = LocalPlayer.Character.HumanoidRootPart.Position.Y - 3.2 -- Lock Y saat dinyalakan
        else
            lockedY = nil
        end
    end 
})
Tabs.Player:Toggle({ Title = "Anti Drown", Callback = function(v) FlowConfig.AntiDrown = v end })
Tabs.Player:Toggle({ Title = "Auto Equip Rod", Callback = function(v) FlowConfig.AutoEquip = v end })

-- [FISHING]
Tabs.Fishing:Toggle({ Title = "Auto Farm", Callback = function(v) FlowConfig.Farming = v end })
Tabs.Fishing:Input({ Title = "Delay", Placeholder = "3.0", Callback = function(v) FlowConfig.Delay = tonumber(v) or 3.0 end })
Tabs.Fishing:Toggle({ Title = "Auto Sell", Callback = function(v) FlowConfig.AutoSell = v end })

-- [TELEPORT]
local Islands = {
    ["Pulau Nelayan"] = Vector3.new(-34, 14, 256), ["Pulau Aquarist"] = Vector3.new(-2032, 13, -833),
    ["Pulau Crystaliz"] = Vector3.new(214, 14, -2706), ["Gua Crystaliz"] = Vector3.new(171, -142, -4938),
    ["Pulau Pedalaman Posei"] = Vector3.new(-861, -367, -1283), ["Megalodon Zone"] = Vector3.new(3125, 9, -1713)
}
local islandNames = {} for name, _ in pairs(Islands) do table.insert(islandNames, name) end

Tabs.Teleport:Dropdown({
    Title = "Search Location",
    Values = islandNames,
    Callback = function(v)
        if Islands[v] and LocalPlayer.Character then
            -- Teleport instan yang aman
            LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(Islands[v] + Vector3.new(0, 5, 0))
        end
    end
})

-- [WEBHOOK]
Tabs.Webhook:Input({ 
    Title = "Webhook URL", Placeholder = "Paste URL...", 
    Callback = function(v) 
        FlowConfig.WebhookURL = v 
        if v:find("discord.com/api/webhooks") then
            WindUI:Notify({Title="Webhook", Content="Connected successfully!", Duration=3})
        end
    end 
})

Tabs.Webhook:Button({
    Title = "Test Webhook", Icon = "solar:letter-bold",
    Callback = function()
        if FlowConfig.WebhookURL == "" then WindUI:Notify({Title="Error", Content="Input URL dulu!", Duration=3}) return end
        local data = {["embeds"]={{["title"]="✅ Webhook Test",["description"]="Webhook terhubung dengan baik!",["color"]=65280}}}
        task.spawn(function() request({Url=FlowConfig.WebhookURL, Method="POST", Headers={["Content-Type"]="application/json"}, Body=HttpService:JSONEncode(data)}) end)
    end
})

Tabs.Webhook:Section({ Title = "Filter Rarity" })
local rarityList = {"Common", "Uncommon", "Rare", "Epic", "Legendary", "Secret", "Mythic"}
for _, rar in ipairs(rarityList) do
    Tabs.Webhook:Toggle({ 
        Title = rar, Value = FlowConfig.WebhookFilters[rar], 
        Callback = function(v) FlowConfig.WebhookFilters[rar] = v end 
    })
end

-- [UTILITIES & PERFORMANCE]
Tabs.Performance:Toggle({ Title = "Real time Stats", Callback = function(v) StatsGui.Enabled = v end })
Tabs.Performance:Toggle({ Title = "Streamer Mode", Callback = function(v) FlowConfig.Streamer = v end })

-- [[ CORE ENGINE ]]
local waterPart = Instance.new("Part")
waterPart.Name = "FlowWaterPlatform"
waterPart.Size = Vector3.new(15, 1, 15)
waterPart.Transparency = 1
waterPart.Anchored = true
waterPart.CanCollide = true
waterPart.Parent = workspace

LocalPlayer.Idled:Connect(function() VirtualUser:CaptureController(); VirtualUser:ClickButton2(Vector2.new()) end)

-- Webhook Logic (Aman tanpa blocking)
LocalPlayer.Backpack.ChildAdded:Connect(function(item)
    if item:IsA("Tool") and FlowConfig.WebhookURL ~= "" then
        task.spawn(function()
            task.wait(0.5)
            local rarity = item:GetAttribute("Rarity") or item:GetAttribute("rarity") or "Common"
            if FlowConfig.WebhookFilters[rarity] then
                local data = {
                    ["embeds"] = {{
                        ["title"] = "🎣 Flow Hub | New Catch!", ["color"] = 0x00B4D8,
                        ["fields"] = {
                            {["name"] = "Fish Name", ["value"] = item.Name, ["inline"] = true},
                            {["name"] = "Rarity", ["value"] = rarity, ["inline"] = true}
                        }, ["footer"] = {["text"] = "Flow Hub v1.2.2"}
                    }}
                }
                pcall(function()
                    request({Url = FlowConfig.WebhookURL, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(data)})
                end)
            end
        end)
    end
end)

-- Physics Loop (High Priority)
getgenv().FlowConnection = RunService.Heartbeat:Connect(function()
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("HumanoidRootPart") then
        local root = char.HumanoidRootPart
        
        -- Walk on Water Anti-Bounce Logic
        if FlowConfig.WalkWater and lockedY then
            waterPart.CFrame = CFrame.new(root.Position.X, lockedY, root.Position.Z)
        else
            waterPart.CFrame = CFrame.new(0, -9999, 0)
        end
    end
end)

-- Logic Loop (Low Priority: Performa lebih ringan)
task.spawn(function()
    local CachedSellRemote = nil
    while task.wait(1) do
        local char = LocalPlayer.Character
        if not char then continue end
        local hum = char:FindFirstChild("Humanoid")
        
        -- Anti Drown (Reset Oxygen)
        if FlowConfig.AntiDrown and hum then
            hum:SetStateEnabled(Enum.HumanoidStateType.Swimming, false)
            for _, v in pairs(char:GetDescendants()) do
                if (v.Name:lower() == "oxygen" or v.Name:lower() == "o2") and (v:IsA("NumberValue") or v:IsA("IntValue")) then
                    v.Value = 100
                end
            end
        elseif hum then
            hum:SetStateEnabled(Enum.HumanoidStateType.Swimming, true)
        end

        -- Auto Equip Rod
        if FlowConfig.AutoEquip and not char:FindFirstChildWhichIsA("Tool") then
            local backpack = LocalPlayer:FindFirstChild("Backpack")
            if backpack then
                for _, item in pairs(backpack:GetChildren()) do
                    if item:IsA("Tool") and (item.Name:lower():find("rod") or item:FindFirstChild("Handle")) then
                        hum:EquipTool(item)
                        break
                    end
                end
            end
        end

        -- Streamer Mode (Hide Nametags)
        if FlowConfig.Streamer then
            for _, p in pairs(Players:GetPlayers()) do
                if p.Character then
                    for _, v in pairs(p.Character:GetDescendants()) do
                        if v:IsA("BillboardGui") or v:IsA("SurfaceGui") then v.Enabled = false end
                        if v:IsA("TextLabel") and (v.Name:lower() == "namelabel" or v.Name:lower() == "name") then v.Visible = false end
                    end
                end
            end
        end

        -- Cache Sell Remote agar tidak lag
        if FlowConfig.AutoSell then
            if not CachedSellRemote and FishingSystem then
                CachedSellRemote = FishingSystem:FindFirstChild("SellFish", true)
            end
            if CachedSellRemote then pcall(function() CachedSellRemote:InvokeServer() end) end
        end
    end
end)

-- Fast Fishing Loop
task.spawn(function()
    while task.wait(0.5) do
        if FlowConfig.Farming and LocalPlayer.Character and FishingSystem then
            pcall(function()
                local tool = LocalPlayer.Character:FindFirstChildWhichIsA("Tool")
                if tool then
                    local root = LocalPlayer.Character.HumanoidRootPart
                    local castPos = root.Position + (root.CFrame.LookVector * 15)
                    if AnimController then AnimController:Play("PrepareCast", 0.1) end
                    
                    local castRep = FishingSystem:FindFirstChild("CastReplication")
                    if castRep then castRep:FireServer(castPos, root.Position, tool.Name, 99.5) end
                    
                    task.wait(1.5)
                    local precalc = FishingSystem:FindFirstChild("PrecalcFish")
                    if precalc then precalc:InvokeServer() end
                    task.wait(0.5)
                    
                    local pullAlert = FishingSystem:FindFirstChild("ReplicatePullAlert")
                    if pullAlert then pullAlert:FireServer("rbxassetid://76503247176490") end
                    
                    task.wait(FlowConfig.Delay)
                    
                    local fishGiver = FishingSystem:FindFirstChild("FishGiver")
                    if fishGiver then fishGiver:FireServer({["hookPosition"]=castPos}) end
                    
                    task.wait(1.5)
                    local cleanCast = FishingSystem:FindFirstChild("CleanupCast")
                    if cleanCast then cleanCast:FireServer() end
                end
            end)
        end
    end
end)
