local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local HttpService = game:GetService("HttpService")
local Stats = game:GetService("Stats")

-- [1] DETEKSI EXECUTOR & VERSI
local Executor = (identifyexecutor and identifyexecutor()) or "Unknown PC/Mobile"
local Version = "v14.0.6 | ATOMIC"
local Developer = "Alfer1427"

-- CLEANUP UI LAMA
for _, v in pairs(game:GetService("CoreGui"):GetChildren()) do
    if v.Name == "WindUI" or v.Name == "DapzXStats_v6" then v:Destroy() end
end

-- [2] WINDUI INITIALIZATION
local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/main/dist/main.lua"))()

_G.FarmingActive = false
_G.AutoSellActive = false
_G.WalkOnWater = false
_G.AntiDrown = false
_G.StreamerMode = false
_G.MinigameDelay = 3.0
_G.WebhookURL = ""

local Window = WindUI:CreateWindow({
    Title = "DapzX Atomic | Fishzar",
    Author = "by " .. Developer,
    Icon = "solar:atom-bold",
    Size = UDim2.fromOffset(580, 460)
})

-- [3] MENAMPILKAN TAG EXECUTOR & VERSI (DI SINI!)
Window:Tag({ 
    Title = Executor .. " | " .. Version, 
    Color = Color3.fromHex("#00B4D8") 
})

-- [4] SEMUA 9 TAB (DIJAMIN MUNCUL)
local MainTab = Window:Tab({ Title = "Main", Icon = "solar:home-bold" })
local InfoTab = Window:Tab({ Title = "Info", Icon = "solar:info-square-bold" })
local PlayerTab = Window:Tab({ Title = "Player", Icon = "solar:user-rounded-bold" })
local FishingTab = Window:Tab({ Title = "Fishing", Icon = "solar:cursor-square-bold" })
local TeleportTab = Window:Tab({ Title = "Teleport", Icon = "solar:map-point-wave-bold" })
local WebhookTab = Window:Tab({ Title = "Webhook", Icon = "solar:bell-bing-bold" })
local ShopTab = Window:Tab({ Title = "Shop", Icon = "solar:cart-bold" })
local UtilTab = Window:Tab({ Title = "Utilities", Icon = "solar:shield-check-bold" })
local PerfTab = Window:Tab({ Title = "Performance", Icon = "solar:monitor-camera-bold" })

-- --- MAIN CONTENT ---
MainTab:Section({ Title = "Executor Info" })
MainTab:Section({ Title = "Running on: " .. Executor, TextSize = 14 })
MainTab:Section({ Title = "Script Version: " .. Version, TextSize = 14 })
MainTab:Button({ Title = "Join Discord: getsades", Callback = function() setclipboard("https://discord.gg/getsades") end })

-- --- INFO CONTENT ---
InfoTab:Section({ Title = "Changelog " .. Version })
InfoTab:Section({ Title = "- DETECTED: Executor & Version visibility\n- RESTORED: All 9 Tabs\n- FIXED: Original Fishing Method (v1)\n- FIXED: Streamer Mode Persistence", TextSize = 13 })

-- --- PLAYER CONTENT ---
PlayerTab:Toggle({ Title = "Walk on Water", Callback = function(v) _G.WalkOnWater = v end })
PlayerTab:Toggle({ Title = "Anti Drown", Callback = function(v) _G.AntiDrown = v end })

-- --- FISHING CONTENT (ORIGINAL METHOD 1) ---
FishingTab:Toggle({ Title = "Auto Farm (Method 1)", Callback = function(v) _G.FarmingActive = v end })
FishingTab:Input({ Title = "Minigame Delay", Placeholder = "3.0", Callback = function(v) _G.MinigameDelay = tonumber(v) or 3.0 end })
FishingTab:Toggle({ Title = "Auto Sell", Callback = function(v) _G.AutoSellActive = v end })

-- --- TELEPORT CONTENT ---
TeleportTab:Dropdown({ Title = "Island Teleport", Values = {"Pulau Nelayan", "Pulau Aquarist", "Pulau Criystializ"}, Callback = function(opt) 
    local locs = {["Pulau Nelayan"] = Vector3.new(-34, 14, 256), ["Pulau Aquarist"] = Vector3.new(-2032, 13, -833), ["Pulau Criystializ"] = Vector3.new(203, 13, -2614)}
    if locs[opt] then LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(locs[opt] + Vector3.new(0,5,0)) end 
end })

-- --- WEBHOOK CONTENT ---
WebhookTab:Input({ Title = "Webhook URL", Callback = function(v) _G.WebhookURL = v end })

-- --- SHOP CONTENT ---
ShopTab:Input({ Title = "Item Name", Callback = function(v) end })

-- --- UTILITIES CONTENT ---
UtilTab:Toggle({ Title = "Streamer Mode", Callback = function(v) _G.StreamerMode = v end })

-- --- PERFORMANCE CONTENT ---
PerfTab:Button({ Title = "FPS Boost", Callback = function() 
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("BasePart") then v.Material = Enum.Material.SmoothPlastic v.CastShadow = false end
    end
end })

-- [5] BACKGROUND LOGIC (STREAMER, WATER, ANTI-DROWN)
task.spawn(function()
    local FishingSystem = ReplicatedStorage:WaitForChild("FishingSystem", 10)
    
    -- WoW Platform
    local WoW = Instance.new("Part", workspace)
    WoW.Size = Vector3.new(30, 1, 30)
    WoW.Anchored, WoW.Transparency, WoW.CanCollide = true, 1, false

    while true do
        task.wait(0.1)
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("HumanoidRootPart") then
            -- Streamer Mode (Aggressive)
            if _G.StreamerMode then
                char.Humanoid.DisplayName = "Streamer"
                for _, p in pairs(Players:GetPlayers()) do
                    if p ~= LocalPlayer and p.Character then p.Character.Humanoid.DisplayName = "Player" end
                end
            end
            
            -- Walk on Water
            if _G.WalkOnWater then
                WoW.CanCollide = true
                WoW.CFrame = CFrame.new(char.HumanoidRootPart.Position.X, 13.8, char.HumanoidRootPart.Position.Z)
            else
                WoW.CanCollide = false
            end

            -- Anti Drown
            if _G.AntiDrown then
                local ox = char:FindFirstChild("Oxygen") or char:FindFirstChild("Breath")
                if ox then ox.Value = 100 end
            end
        end
    end
end)

-- [6] AUTO FARM LOOP (ORIGINAL METHOD 1)
task.spawn(function()
    local FS = ReplicatedStorage:WaitForChild("FishingSystem", 5)
    while true do
        task.wait(0.5)
        if _G.FarmingActive and FS then
            pcall(function()
                local char = LocalPlayer.Character
                local rod = char:FindFirstChildWhichIsA("Tool")
                if rod then
                    local root = char.HumanoidRootPart
                    local castPos = root.Position + (root.CFrame.LookVector * 15)
                    FS.CastReplication:FireServer(castPos, root.Position, rod.Name, 100)
                    task.wait(2)
                    FS.PrecalcFish:InvokeServer()
                    task.wait(_G.MinigameDelay)
                    FS.FishGiver:FireServer({["hookPosition"] = castPos})
                    FS.CleanupCast:FireServer()
                end
            end)
        end
    end
end)

WindUI:Notify({ Title = "DapzX Atomic Loaded", Content = "Running on " .. Executor, Duration = 5 })
